fix(chat): short-circuit chitchat; wire EvaluationAgent into ChatService dispatch

- Short-circuit CHITCHAT/OFF_TOPIC/INVALID intents at the top of
  ChatService.chat() — return fixed greeting, no CV load, no Qwen call.
- Dispatch SKILL_GAP_ADVICE and SELF_EVALUATE intents to
  EvaluationAgent via dispatch_evaluation, loading the user's default CV.
- Remove CHITCHAT from route_after_kg in recommend graph — it's already
  intercepted upstream.
- Add 23 unit tests covering all three dispatch paths (chitchat,
  evaluation, recommend).
